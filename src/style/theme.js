const size = {
  mobile: "800px",
  desktop: "1920px",
};
const theme = {
  mainColor: "#white",
  mobile: `(max-width:${size.mobile})`,
  desktop: `(max-width:${size.desktop})`,
};
export default theme;
